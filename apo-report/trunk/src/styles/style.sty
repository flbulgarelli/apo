\ProvidesPackage{mystyle}

%\usepackage{times}
%\usepackage{pslatex}
\usepackage{newcent}

%\\usepackage[ansinew]{inputenc}
\usepackage[spanish]{babel}
\usepackage[utf8]{inputenc}

\usepackage{amssymb}
\usepackage{hyperref}
\usepackage[fixlanguage]{babelbib}
\selectbiblanguage{spanish}
\usepackage{fullpage}
\usepackage{array}
\usepackage{amssymb, amsmath}
\usepackage{color}
\usepackage{tabularx}
\usepackage{fancybox}
\usepackage{fancyhdr}
\usepackage{longtable}
\usepackage{acronym}
\usepackage{listings}

%\usepackage{natbib}
\usepackage{makeidx}  % allows for indexgeneration

%\usepackage[colorlinks]{hyperref}
\usepackage{hyperref}
\hypersetup{
    colorlinks,
    citecolor=black,
    filecolor=black,
    linkcolor=black,
    urlcolor=black
}


\renewcommand{\tablename}{Tabla}

\newenvironment{myitemize}{%
     \begin{itemize} \setlength{\itemsep}{0cm}}
     {\end{itemize}}

\newenvironment{myenumerate}{%
     \begin{enumerate} \setlength{\itemsep}{0cm}}
     {\end{enumerate}}

\usepackage[pdftex]{graphicx}


\clubpenalty=10000
\widowpenalty=10000
%\raggedbottom


\setlongtables

\definecolor{javared}{rgb}{0.6,0,0} % for strings
\definecolor{javagreen}{rgb}{0.25,0.5,0.35} % comments
\definecolor{javapurple}{rgb}{0.5,0,0.35} % keywords
\definecolor{javadocblue}{rgb}{0.25,0.35,0.75} % javadoc

\lstset{
	frame=tb,
	language=Java,
	aboveskip=3mm,
  	belowskip=3mm,
  	columns=flexible,
	basicstyle=\ttfamily,
	keywordstyle=\color{javapurple}\bfseries,
	stringstyle=\color{javared},
	commentstyle=\color{javagreen},
	morecomment=[s][\color{javadocblue}]{/**}{*/},
	numbers=none,
	numberstyle=\tiny\color{black},
	stepnumber=2,
	numbersep=10pt,
	showspaces=false,
	showstringspaces=false,
	tabsize=4
}

\newif\ifshowfigs
\ifx\hideFigs\undefined
  \showfigstrue         % show figures for complete document
\else
  \showfigsfalse        % hide figures for fast preview
\fi
\newif\ifpdf
\ifx\pdfoutput\undefined
  \pdffalse         % we are not running PDFLaTeX
\else
  \pdfoutput=1          % we are running PDFLaTeX
  \pdftrue
\fi \ifpdf
  \pdfcompresslevel=9
  \DeclareGraphicsExtensions{.png,.jpg,.pdf,.mps,.eps,.gif,.ps}
\else
  \DeclareGraphicsExtensions{.eps,.ps}
\fi

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\newcommand{\comment}[1]{{\color{blue}#1}}

% otras definiciones

% redefinicion de algunas titulos:

\addto{\captionsspanish}{\def\tablename{Tabla}}
\addto{\captionsspanish}{\def\figurename{Figura}}
\addto{\captionsspanish}{\def\listtablename{Indice de tablas}}
\addto{\captionsspanish}{\def\contentsname{Indice}}
\addto{\captionsspanish}{\def\bibliographyname{Fuentes consultadas}}
\spanishdecimal{.}

% numeracion romana en minusculas (ok, falta de ortografia, pero estoy clonando el estilo oficial :S)

\makeatletter
\addto\layoutspanish{\def\@roman#1{\romannumeral #1}}
\makeatother

% detalles de encabezados:

% ancho de encabezado
\setlength{\textwidth}{15.5cm}
\setlength{\headwidth}{\textwidth}

\pagestyle{fancy}

% pone el nombre de los capitulos en el encabezado sin punto al final, y en minusculas.
%\renewcommand{\chaptermark}[1]{
%   \markboth{\chaptername\ \thechapter\ \ #1}{}
%}

% idem para secciones
\renewcommand{\sectionmark}[1]{
  \markright{\thesection\ \ #1}}

% contenido de encabezados
\fancyhead{}
\fancyhead[LE,RO]{\rm \bf \thepage}  % numero de pagina
\fancyhead[RE]{\sl \leftmark}        % el nombre del capitulo
\fancyhead[LO]{\sl \rightmark}       % el nombre de la seccion
\fancyfoot{}                         % nada en el pie de pagina

% el grosor de la linea decorativa
\renewcommand{\headrulewidth}{0.3pt}

% la linea decorativa un poco mas abajo.
\renewcommand{\headrule}{
   \vspace*{-0.25cm} \vbox to 0pt{\hbox to\headwidth{\hrulefill}}
}

% redefine cleardoublepage para que las paginas pares vacias al final de cada capitulo
% queden realmente vacias:
\makeatletter
\def\cleardoublepage{
   \clearpage\if@twoside
   \ifodd\c@page\else \hbox{}
   \vspace*{\fill}
   \vspace{\fill}
   \thispagestyle{empty}
   \newpage \if@twocolumn\hbox{}\newpage\fi\fi\fi
}
\makeatother

% otras definiciones

\setlength{\textheight}{22.5cm}
\setlength{\textwidth}{15.5cm}
\setlength{\topmargin}{-0.8cm}
\setlength{\evensidemargin}{0cm}
\setlength{\oddsidemargin}{1.02cm}
\setlength{\headsep}{0.5in}
\setlength{\floatsep}{12pt plus 2pt minus 4pt}
\setlength{\textfloatsep}{20pt plus 2pt minus 4pt}
\setlength{\intextsep}{10pt plus 2pt minus 3pt}

